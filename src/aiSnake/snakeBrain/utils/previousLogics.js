/* Loop around map corners: */
// if((snakeHead.x + 10) === MAP_SIZE_INFO.MAP_WIDTH && direction.x > 0) {
//         e = simulateKeyPress('down')
// } else if (snakeHead.x === 0 && direction.x < 0) {
//     e = simulateKeyPress('up')
// } else if ((snakeHead.y + 10) === MAP_SIZE_INFO.MAP_HEIGHT && direction.y > 0) {
//     e = simulateKeyPress('left')
// } else if (snakeHead.y === 0  && direction.y < 0) {
//     e = simulateKeyPress('right')
// }
